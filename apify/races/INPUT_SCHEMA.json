{
  "title": "Input schema for the races actor.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "number_of_pages": {
      "title": "Number of race pages to scan",
      "type": "string",
      "description": "Number of race pages to scan",
      "editor": "textfield"
    },
    "use_cache": {
      "title": "Whether to use already scraped results",
      "type": "boolean",
      "description": "Whether to use already scraped results",
      "editor": "checkbox"
    }},
  "required": []
}
